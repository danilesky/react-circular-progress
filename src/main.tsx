import React from "react";
import ReactDOM from "react-dom/client";
import Plaground from "./Playground.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Plaground />
  </React.StrictMode>
);
